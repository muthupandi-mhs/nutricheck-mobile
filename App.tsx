/**
 * Entry point. The app itself lives in `src/` — this file exists because
 * `index.js` and the Jest preset both expect an `App.tsx` at the root.
 *
 * @format
 */
export { default } from './src/App';
